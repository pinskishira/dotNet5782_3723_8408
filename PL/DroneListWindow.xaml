<Window x:Class="PL.DroneListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="DroneListWindow" Height="640" Width="800">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="400*"/>
        </Grid.RowDefinitions>
        <Grid Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" Grid.Row="0"
         VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >
            <ComboBox x:Name="StatusSelection" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="136" Height="50"/>
            <ComboBox x:Name="WeightSelection" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="136" Height="50"/>
            <Label Content="Label" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Height="26" Width="87"/>
            <Label Content="Label" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="26" Width="87"/>
        </Grid>
        <ListView x:Name ="DronesListView" Margin="10,10,10,10" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
